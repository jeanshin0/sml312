# ramp_dataset.yaml

# Root directory of dataset
path: ../datasets/ramp_dataset

# Relative paths from `path` to the splits
train: images/train
val:   images/val

# Number of classes
nc: 2

# Class names
names:
  0: ramp
  1: stairs